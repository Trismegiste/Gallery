<div class="pure-g-r centering">
    {% for idx, entry in category %}
    <div class="pure-u-1-3">
        <a href="#{{ idx }}" id="cat{{ idx }}">
            <img src="{{ entry }}"/>
        </a>
    </div>
    {% endfor %}
</div>